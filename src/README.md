# src/

Directorio reservado para la implementación de código fuente del MVP.

## Objetivo inmediato
- Centralizar aquí la lógica ejecutable que hoy está definida en contratos y documentación.
- Mantener separación con `docs/` (especificación) y `storage/migrations/` (esquema).

## Siguiente paso sugerido
- Mover gradualmente la implementación de CLI/runtime a `src/` manteniendo compatibilidad con el contrato `docs/contracts/app_cli_contract_v0.md`.
